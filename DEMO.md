# Demo

The Demo feature (`/demo`) allows quick toggling of fake transaction loads, mock mesh network nodes, and forced gateway behavior. This is intended to test visualization without needing 10 physical devices.
